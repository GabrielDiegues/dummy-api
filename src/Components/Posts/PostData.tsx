import { Post } from "@/types";
import { Text } from "react-native";

type PostDataProps = {
    post: Post;
}

const PostData = ({post}: PostDataProps) => {
    return (
        
        <Text></Text>
    );
}
export default PostData;